package dev.abstratium.conditions.entity;

public enum ContractState {
    DRAFT,
    OFFERED,
    ACCEPTED,
    AWAITING_APPROVAL,
    APPROVED,
    AWAITING_PAYMENT,
    RUNNING,
    CANCELLED,
    EXPIRED,
    TERMINATED
}
