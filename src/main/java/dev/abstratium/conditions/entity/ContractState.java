package dev.abstratium.conditions.entity;

public enum ContractState {
    DRAFT,
    OFFERED,
    ACCEPTED,
    AWAITING_APPROVAL,
    APPROVED,
    RUNNING,
    CANCELLED,
    EXPIRED,
    TERMINATED
}
